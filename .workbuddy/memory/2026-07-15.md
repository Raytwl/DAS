# 2026-07-15 Work Log

## Task: Generate figures from real training data

### Completed:
1. User reported training finished in WSL Ubuntu
2. Located training data at \\wsl.localhost\Ubuntu\home\tan\DAS\store_data\
3. Copied all .dat files to Windows store_data/ directory
4. Analyzed training data:
   - Problem: 2D Peak (probsetup=3), DAS-G mode (replace_all=0)
   - 5 stages, 45000 total PDE iterations, 30000 flow iterations
   - Stage boundaries: [0, 3000, 9000, 18000, 30000, 45000]
   - Validation error: 0.042 -> 0.00057
   - u_pred MSE: 0.000574, matches peak solution
5. Updated das_plot.py to properly handle real data:
   - Solution comparison now loads u_pred.dat (256x256 grid)
   - Error curves use real validation_error.dat
   - Adaptivity steps split by detected stage boundaries
   - Training set evolution uses real stage resample data
   - Loss curves use real pdeloss/residualloss/entropyloss data
   - Added new figures: full error curve with stage markers,
     residual loss with stage boundaries highlighted
6. Generated all figures with real data successfully

### Key observations from data:
- Stage 2 resample points concentrate around (0.5, 0.5) = peak location
  This confirms adaptive sampling is working correctly
- Error decreases consistently across all 5 stages
- Entropy loss shows flow model converging (negative values expected)

## Task: Rewrite das_plot.py organized by figure number

### Completed:
1. Re-read paper pages 15-28 for all experiment settings
2. Created run_experiments.sh with all training commands for 18 figures
3. Rewrote das_plot.py:
   - Organized by figure number (fig01-fig18)
   - Each function documents what experiment data it needs
   - Loads from experiments/{problem}_{method}_{size}/ directories
   - Falls back to demo data with warning if data missing
   - Supports --fig "1-4" or "1 2 3" to generate specific figures
4. Experiment data structure: experiments/{problem}_{method}_{|S_Omega|}/

### Key paper settings:
- 2D Peak: NN 6L/32neuron, KRnet L=6/24neuron, Ne=3000, batch=500
- 2D Bimodal: NN 6L/64neuron, KRnet L=8/48neuron, Ne=5000, batch=500
- 10D Linear: NN 6L/64neuron, KRnet K=3/L=6/64neuron, Ne=3000, batch=5000
- RAR method NOT in original code (needs separate implementation)
- 10D Nonlinear PDE NOT in original code (needs to be added)
