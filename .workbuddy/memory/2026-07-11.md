# 2026-07-11 Work Log

## Task: Read DAS-PINN paper figures and create visualization script

### Completed:
1. Read DAS_PINN.pdf (35 pages) using PyMuPDF to extract text and figure captions
2. Identified all 18 figures in the paper:
   - Figs 1-4: 2D Peak problem (error curves, solution comparison, adaptivity steps, training set evolution)
   - Figs 5-7: 2D Bimodal problem (same figure types)
   - Figs 8-13: 10D Linear problem (convergence, error curves, training set evolution, variance)
   - Figs 14-18: 10D Nonlinear problem (same figure types as linear)
3. Created das_plot.py visualization script:
   - Covers all 18 figure types from the paper
   - Custom color palette (different from paper to avoid plagiarism)
   - Supports --demo mode with synthetic data for preview
   - Reads from store_data/ when real training data available
   - argparse interface similar to das_train.py
   - Bonus figures: loss curves, 3D exact solution surface
4. Tested script in demo mode for all 3 problem types (peak, bimodal, 10D exp)
5. All figures generated successfully to ./figures/ directory

### Key Decisions:
- Used custom colormap (blue-orange diverging) instead of paper's viridis
- Used different markers (square, triangle, diamond, v) for different methods
- Added 3D surface plots and loss curves as bonus figures for report
- Script is independent of TensorFlow (only needs matplotlib+numpy)
